# UNI-T UT117C — `ut117c`

> **State:** `untested` (ported, not bench-tested; sample vectors synthesized). **Driver:** `packages/protocol/src/drivers/ut117c.ts`. **Source:** ported from `webspiderteam/Bluetooth-DMM-For-Windows`.

## Overview

The UT117C is a UNI-T digital multimeter / clamp-capable meter. Unlike the streaming `uni-t` family, this model is a **polled (request/response) AB-CD meter**: the meter never pushes data on its own. This driver repeatedly writes a *"request realtime data"* command and the meter answers with **exactly one frame per request** (`ut117c.ts:7-11`).

The driver was ported from the upstream `webspiderteam/Bluetooth-DMM-For-Windows`. It is marked `verification: 'ported-unverified'` (`ut117c.ts:309`) because it has not been validated against physical hardware; the function-label table is best-effort, not authoritative.

Key shape:
- Transport: shared ISSC "Transparent UART" GATT service, but writes go to **WriteUUID2** (`…8841…`) (`ut117c.ts:7-8`, `311-313`).
- Framing: generic AB-CD framing — `AB CD`, big-endian 16-bit length, payload, trailing 16-bit big-endian **additive** checksum (`ut117c.ts:13-18`).
- Decode never throws: a short/garbled/mistyped frame yields a blank `Reading` (`ut117c.ts:65-90`, `137-138`).

## Models   <UT117C; BLE name prefix>

| Field | Value | Ref |
| --- | --- | --- |
| Driver `id` | `ut117c` | `ut117c.ts:307` |
| `label` | `UT117C Digital Multimeter` | `ut117c.ts:308` |
| `namePrefixes` | `['UT117C']` | `ut117c.ts:310` |
| `match()` | `ctx.name?.startsWith('UT117C')` | `ut117c.ts:315` |
| `verification` | `ported-unverified` | `ut117c.ts:309` |

A single advertised model. Identification is **name-routed**: the registry picks this driver by the `UT117C` name prefix because the GATT service is shared with the rest of the UNI-T family (`ut117c.ts:311-312`).

## Transport (GATT)   <ISSC service + UUIDs; name-routed>

The shared ISSC "Transparent UART" profile (`ut117c.ts:302-304`, `313`):

| Role | UUID | Constant |
| --- | --- | --- |
| Service | `49535343-fe7d-4ae5-8fa9-9fafd205e455` | `ISSC_SERVICE` |
| Notify | `49535343-1e4d-4bd9-ba61-23c647249616` | `ISSC_NOTIFY` |
| Write | `49535343-8841-43f4-a8d4-ecbe34729bb3` | `ISSC_WRITE` (WriteUUID2 `…8841…`) |

```ts
gatt: { service: ISSC_SERVICE, notify: ISSC_NOTIFY, write: [ISSC_WRITE] }   // ut117c.ts:313
```

Notes:
- **Every write** uses the `…8841…` characteristic (WriteUUID2), not the `…8888…`/other write characteristic used by the streaming `uni-t` driver (`ut117c.ts:7-8`, `311`).
- Because several UNI-T models share `ISSC_SERVICE`, the registry's ISSC name-routing disambiguates by `namePrefixes` / `match()`. A first-frame `sniff` (below) is also provided to resolve a service collision on the raw byte stream.

## Handshake / session start   <polled request/response>

There is **no persistent handshake**. The meter answers one frame per poll, so the driver simply **kicks the poll loop once** and relies on the session keep-alive + `onRequest` to re-arm:

```ts
// handshake — kick the poll once (ut117c.ts:321-323)
async handshake(io) {
  await io.write(CMD.POLL);   // AB CD 00 04 05 00 01 81
}

// onRequest — re-send POLL on every answer (measurement frame) or control ACK (ut117c.ts:328-331)
onRequest(frame, io) {
  void frame;
  void io.write(CMD.POLL);
}
```

This mirrors the upstream poll loop and the *"ACK resumes polling"* behaviour (`ut117c.ts:326-327`). Frames classified as either `measurement` (type 2) or `control` (type 1 ACK) drive the next poll, keeping the request/response cycle alive.

### Command frame construction

Each control/poll frame is 8 bytes, built byte-for-byte by `control(cmd, arg)` (`ut117c.ts:42-47`):

```
AB CD 00 04 <cmd> <arg> 01 <chk>
[0] [1] [2][3] [4]   [5]   [6]  [7]
```

- `[0..1]` = `AB CD` sync header (`SOF_H`/`SOF_L`, `ut117c.ts:29-30`).
- `[2..3]` = `00 04` big-endian length.
- `[4]` = `cmd`, `[5]` = `arg`.
- `[6]` = `0x01` trailing marker (a literal constant, **excluded** from the checksum).
- `[7]` = checksum = **low byte** of the 16-bit additive sum of the six bytes `b[0..5]` (header + length + cmd + arg) — `(SOF_H + SOF_L + 0x00 + 0x04 + cmd + arg) & 0xff` (`ut117c.ts:38-46`).

The checksum *deliberately excludes the trailing `0x01` marker*. Verified in source against the captured poll `0x81` (sum `b[0..5] = 0x181`) and hold `0xE8` frames; all nine commands round-trip (`ut117c.ts:40-41`).

### The nine poll/control command byte arrays

From the `CMD` table (`ut117c.ts:51-61`), exported as `UT117C_COMMANDS` (`ut117c.ts:63`):

| Name | `control(cmd,arg)` | Bytes | Meaning |
| --- | --- | --- | --- |
| `POLL` | `0x05, 0x00` | `AB CD 00 04 05 00 01 81` | request realtime data |
| `SELECT` | `0x01, 0x5a` | `AB CD 00 04 01 5A 01 D7` | function/mode select |
| `RANGE` | `0x02, 0x01` | `AB CD 00 04 02 01 01 7F` | enter manual range |
| `RANGE_AUTO` | `0x02, 0x00` | `AB CD 00 04 02 00 01 7E` | exit → auto range |
| `REL` | `0x03, 0x5a` | `AB CD 00 04 03 5A 01 D9` | relative |
| `MAXMIN` | `0x04, 0x01` | `AB CD 00 04 04 01 01 81` | enter MAX/MIN |
| `HOLD` | `0x12, 0x5a` | `AB CD 00 04 12 5A 01 E8` | data hold |
| `LPF` | `0x14, 0x5a` | `AB CD 00 04 14 5A 01 EA` | low-pass filter toggle |
| `BACKLIGHT` | `0x15, 0x5a` | `AB CD 00 04 15 5A 01 EB` | backlight |

## Frame format

Generic AB-CD framing, big-endian length + 16-bit additive checksum (`ut117c.ts:13-24`):

```
[0]   = 0xAB                    sync header high (SOF_H)
[1]   = 0xCD                    sync header low  (SOF_L)
[2..3]= LEN (big-endian)        payload byte count starting at the type byte b[4]
[4]   = packet type             1 = command ACK, 2 = realtime measurement data (TYPE_DATA)
…payload…
last 2 bytes = checksum = Σ(all preceding bytes) & 0xFFFF, big-endian
```

For a **TYPE-2 measurement frame**, `LEN = 0x12` (18) and the total frame is **24 bytes** (`DATA_LEN = 0x12`, `DATA_TOTAL = 4 + 18 + 2 = 24`, `ut117c.ts:35-36`):

| Offset | Field | Notes (`ut117c.ts:19-24`) |
| --- | --- | --- |
| `[0..1]` | `AB CD` header | |
| `[2..3]` | `00 12` length | big-endian = 18 |
| `[4]` | type = `2` | `TYPE_DATA` (`ut117c.ts:32`) |
| `[5]` | `funcID` | function code → label map |
| `[6]` | `rangIndex` | ASCII digit (`value − 0x30`); also NCV strength selector |
| `[7]` | `hzRangIndex` | Hz range index (not surfaced by driver) |
| `[8..14]` | 7-byte ASCII value string | space-padded, may have leading `-` |
| `[15..17]` | 3-byte ASCII unit string | `'o'` → `'Ω'` remap |
| `[18]` | `olFlag` | `0x31`='OL', `0x32`='-OL' |
| `[19]` | `maxminFlag` | max/min record mode (not surfaced as a flag here, `ut117c.ts:207`) |
| `[20]` | `flag1` | hold / rel / lowBattery bits |
| `[21]` | `flag2` | AC/DC / auto / hvWarning bits |
| `[22..23]` | checksum | 16-bit big-endian additive over `b[0..21]` |

### Framer / sync / resync (`Ut117cFramer`, `ut117c.ts:231-275`)

- Accumulate bytes, **sync** on `AB CD` (discard non-`AB` bytes; if `b[1] != CD`, drop and re-sync, `ut117c.ts:262-274`).
- Need ≥ 5 bytes (`AB CD LEN_H LEN_L type`) before reading length (`ut117c.ts:239`).
- `len = (b[2] << 8) | b[3]`; `total = 4 + len + 2` (`ut117c.ts:240-241`).
- Bogus length guard: if `len < 1` or `total > 64`, shift one byte and re-sync (false header in noise, `ut117c.ts:242-245`).
- Wait if `buf.length < total` (frame split across notifications, `ut117c.ts:246`).
- **Checksum gate on measurement frames only:** if `frame[4] === TYPE_DATA` and `!checksumOk(frame)`, shift one byte and re-sync rather than emit a corrupt reading (`ut117c.ts:248-251`).
- Tolerates split *and* coalesced notifications; loops to emit multiple frames per `push` (`ut117c.ts:237-256`).

### Checksum (`checksumOk`, `ut117c.ts:278-286`)

16-bit big-endian additive checksum over **every byte preceding the trailing 2 checksum bytes**:

```
sum = (Σ frame[0 .. len-3]) & 0xFFFF
ok  = (sum >> 8) == frame[len-2]  &&  (sum & 0xFF) == frame[len-1]
```

Returns `false` for frames shorter than 3 bytes.

### Classification (`classify`, `ut117c.ts:222-224`)

```
frame[4] === TYPE_DATA (2) → 'measurement'
otherwise (incl. type 1 ACK) → 'control'
```

Both kinds trigger an `onRequest` re-poll.

## Decode   <funcID→name map, flag decode, LPF folded into ACV>

`decodeUt117c(bytes, ts=0)` (`ut117c.ts:137-218`). Pure and tolerant: returns `blank(ts)` (`ut117c.ts:66-90`) unless **all** of these hold (`ut117c.ts:138-140`):

- `bytes.length >= DATA_TOTAL` (24),
- `bytes[0] === 0xAB && bytes[1] === 0xCD`,
- `bytes[4] === TYPE_DATA` (2).

### funcID → function label (`FUNCTIONS`, `ut117c.ts:95-115`)

Derived from the per-function feature switches in the upstream source. The authoritative display strings are not available, so labels are best-effort (`ut117c.ts:92-94`).

| funcID | label | note |
| --- | --- | --- |
| 1 | `ACV` | |
| 2 | `ACV` | ACV + LPF (LPF folded into ACV) |
| 3 | `ACV` | ACV small range |
| 4 | `DCV` | |
| 5 | `DCV` | |
| 6 | `OHM` | |
| 7 | `CONT` | |
| 8 | `DIODE` | |
| 9 | `CAP` | |
| 10 | `LozV` | Auto V (LoZ) — AC or DC per flag2 |
| 11 | `Hz` | |
| 12 | `Hz` | Hz + LPF |
| 13 | `NCV` | strength label, not numeric |
| 14 | `DCA` | |
| 15 | `ACA` | |
| 16 | `Hz` | current AC + Hz |
| 17 | `ACA` | clamp AC |
| 18 | `ACA` | clamp AC |
| 19 | `DCA` | clamp DC |

Unknown `funcID` falls back to `#<funcID>` (`ut117c.ts:148`). **LPF is folded into `ACV`** (funcIDs 2 and 12 do not produce a distinct "LPF" label) (`ut117c.ts:97-98`, `108-109`).

### Value & unit strings

- `valueStr = asciiString(bytes, 8, 7, remapOhm=false)` — 7 ASCII bytes, trimmed (`ut117c.ts:157`).
- `displayUnit = asciiString(bytes, 15, 3, remapOhm=true)` — 3 ASCII bytes; `'o'` (`0x6f`) → `'Ω'` (`ut117c.ts:158`, `121-129`).
- `asciiString` stops at the first NUL (`0x00`) byte (all-zero is treated as empty) and trims whitespace (`ut117c.ts:117-129`).

Numeric test: `NUMERIC = /^-?\d*\.?\d+$/` (`ut117c.ts:131`); `numeric = valueStr.length > 0 && NUMERIC.test(valueStr)` (`ut117c.ts:182`).

### Overload (`ut117c.ts:183-190`)

Overload applies **only to numeric readings** — `olFlag` overrides the value string:

```
overload    = numeric && (olFlag === 0x31 || olFlag === 0x32)
displayText = overload ? (olFlag === 0x32 ? '-OL' : 'OL') : valueStr
displayValue = (numeric && !overload) ? Number(valueStr) : null
```

### Base-value normalization (`ut117c.ts:192-193`)

```
{ base: baseUnit, exp } = unitInfo(displayUnit)
baseValue = displayValue === null ? null : displayValue * 10 ** exp
```

`unitInfo` (`types.ts:168-174`) splits a metric prefix (`n/µ/m/k/M` → `−9/−6/−3/3/6`) off the unit only when a base char remains; e.g. `kΩ` → base `Ω`, exp `3`; bare `V`/`Ω`/`%` stay put.

### AC/DC (`ut117c.ts:150-155`)

```
isAc = (flag2 & 0x02) !== 0           // setDc = ((flag2 & 2) == 0)
acdcRelevant = fn ∈ {ACV, DCV, DCA, ACA, LozV}
acdc = acdcRelevant ? (isAc ? 'AC' : 'DC') : ''
```

AC/DC is only emitted for the V/A functions and LoZ auto-V; everything else reports `''`.

### Flag decode (`flag1` = `b[20]`, `flag2` = `b[21]`)

Standard (non-NCV) path (`ut117c.ts:206-216`) and NCV path (`ut117c.ts:168-178`) share the same bit assignments:

| Flag | Source bit | Ref |
| --- | --- | --- |
| `hold` | `flag1 & 0x02` | `ut117c.ts:209` |
| `rel` | `flag1 & 0x01` | `ut117c.ts:210` |
| `lowBattery` | `flag1 & 0x08` | `ut117c.ts:212` |
| `auto` | `flag2 & 0x08` | `ut117c.ts:211` |
| `hvWarning` | `flag2 & 0x01` | `ut117c.ts:213` |
| `max` / `min` | always `false` | record state in `b[19]` is a mode, not surfaced (`ut117c.ts:207-208`) |
| `peakMax` / `peakMin` | always `false` | `ut117c.ts:214-215` |

`bargraph` is always `0` (no analog bar in this protocol, `ut117c.ts:205`).

### NCV special case (funcID 13, `ut117c.ts:161-180`)

NCV shows a **strength label, not a number**:

```
displayText = (rangIndex === 0x31 /* '1' */) ? 'HI' : 'LO'
displayUnit = ''
function    = 'NCV'
displayValue = null, baseValue = null   (via blank())
```

Flags are still decoded (hold/rel/auto/lowBattery/hvWarning) using the same bits as above.

## Controls   <the command byte arrays>

Front-panel soft buttons, mapped to the generic `MeterControl` keys (`ut117c.ts:336-344`):

| Control key | CMD | Bytes |
| --- | --- | --- |
| `backlight` | `BACKLIGHT` | `AB CD 00 04 15 5A 01 EB` |
| `hold` | `HOLD` | `AB CD 00 04 12 5A 01 E8` |
| `rel` | `REL` | `AB CD 00 04 03 5A 01 D9` |
| `select` | `SELECT` | `AB CD 00 04 01 5A 01 D7` |
| `range` | `RANGE` | `AB CD 00 04 02 01 01 7F` |
| `rangeAuto` | `RANGE_AUTO` | `AB CD 00 04 02 00 01 7E` |
| `maxMin` | `MAXMIN` | `AB CD 00 04 04 01 01 81` |

`LPF` (`AB CD 00 04 14 5A 01 EA`) and `POLL` are defined in `CMD` but **not** wired into the generic `controls` map (there is no `MeterControl` key for LPF; POLL is the internal keep-alive). Both are still reachable via the exported `UT117C_COMMANDS` (`ut117c.ts:63`).

The `hzDuty` control is **not** provided (no Hz/duty soft button mapped for this model).

## Sniffer

`looksLikeUt117cFrame(bytes)` resolves the shared-ISSC-service collision on the raw first frame (`ut117c.ts:290-300`, wired as `sniff` at `ut117c.ts:346`). Returns `true` iff all hold:

```
bytes.length === 24 (DATA_TOTAL)
bytes[0] === 0xAB
bytes[1] === 0xCD
bytes[2] === 0x00
bytes[3] === 0x12 (DATA_LEN)
bytes[4] === 2    (TYPE_DATA)
checksumOk(bytes)
```

## Verification   <untested>

- **State:** `untested` — `verification: 'ported-unverified'` (`ut117c.ts:309`). Not bench-tested on physical hardware (`ut117c.ts:4-5`).
- **Labels:** the `funcID → name` table is **derived** from per-function feature switches in the upstream source; the authoritative display strings are not available, so labels are best-effort (`ut117c.ts:92-94`).
- **Command bytes:** the checksum helper is verified in source against the captured `POLL` (`0x81`, sum `b[0..5]=0x181`) and `HOLD` (`0xE8`) frames; all nine commands round-trip in tests (`ut117c.ts:40-41`, `49-50`).
- **Sample vectors:** synthesized (no hardware captures of measurement frames).
- **Confidence:** medium. Transport/framing/checksum and command bytes are well-grounded; field offsets and function/flag semantics are ported but unconfirmed on hardware.

## Source   <driver file + upstream fn>

- Driver: `packages/protocol/src/drivers/ut117c.ts`.
- Shared types / `unitInfo`: `packages/protocol/src/types.ts`.
- Driver interface / `DriverFramer` / `ParsedFrame`: `packages/protocol/src/drivers/types.ts`.
- Upstream port: `webspiderteam/Bluetooth-DMM-For-Windows`.
